rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 0. Global Safety Net & Admin Bypass
    // This allows the platform administrator ry9506602867@gmail.com to perform
    // read and write operations on any path globally, ensuring backup and wipe system operations succeed.
    match /{document=**} {
      allow read, write: if isSignedIn() && isAdmin();
    }

    // ===============================================================
    // Helper Functions
    // ===============================================================
    
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    function incoming() {
      return request.resource.data;
    }

    function existing() {
      return resource.data;
    }

    function isAdmin() {
      return isSignedIn() && request.auth.token.email == "ry9506602867@gmail.com";
    }

    function getUserData() {
      let path = /databases/$(database)/documents/users/$(request.auth.uid);
      return exists(path) ? get(path).data : {'role': 'guest', 'ownerId': ''};
    }

    function hasRole(role) {
      let data = getUserData();
      return isSignedIn() && data.get('role', '') == role;
    }

    function isStaffOf(ownerId) {
      let data = getUserData();
      return isSignedIn() && (data.get('ownerId', '') == ownerId || request.auth.uid == ownerId);
    }

    function canAccess(ownerId) {
      return isOwner(ownerId) || isStaffOf(ownerId) || isAdmin();
    }

    // ===============================================================
    // Collection Rules
    // ===============================================================

    match /users/{userId} {
      allow get: if isSignedIn() && (isOwner(userId) || isAdmin());
      allow list: if isSignedIn() && isAdmin();
      allow create: if isSignedIn() && isOwner(userId);
      allow update, delete: if isSignedIn() && (isOwner(userId) || isAdmin());
    }

    match /profiles/{userId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && isOwner(userId);
      allow update, delete: if isSignedIn() && (isOwner(userId) || isAdmin());
    }

    match /business_profiles/{userId} {
      allow read: if isSignedIn();
      allow create, update, delete: if isSignedIn() && (isOwner(userId) || isAdmin());
    }

    match /products/{productId} {
      allow get: if isSignedIn() && canAccess(resource.data.userId);
      allow list: if isSignedIn() && (canAccess(resource.data.userId) || resource.data.userId == request.auth.uid);
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow update: if isSignedIn() && canAccess(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /categories/{categoryId} {
      allow get: if isSignedIn() && canAccess(resource.data.userId);
      allow list: if isSignedIn() && (canAccess(resource.data.userId) || resource.data.userId == request.auth.uid);
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow update: if isSignedIn() && canAccess(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /b2b_products/{productId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && isOwner(incoming().userId);
      allow update: if isSignedIn() && isOwner(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /invoices/{invoiceId} {
      allow get: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow list: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow update: if isSignedIn() && canAccess(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /quotations/{invoiceId} {
      allow get: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow list: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow update: if isSignedIn() && canAccess(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /payments/{paymentId} {
      allow get: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow list: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow update: if isSignedIn() && canAccess(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /expenses/{expenseId} {
      allow get: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow list: if isSignedIn() && (canAccess(resource.data.userId) || hasRole('ca'));
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow update: if isSignedIn() && canAccess(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /staff/{staffId} {
      allow get: if isSignedIn();
      allow list: if isSignedIn();
      allow create: if isSignedIn() && isOwner(incoming().userId);
      allow update: if isSignedIn() && isOwner(existing().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    match /activities/{activityId} {
      allow get: if isSignedIn() && canAccess(resource.data.userId);
      allow list: if isSignedIn() && canAccess(resource.data.userId);
      allow create: if isSignedIn() && canAccess(incoming().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }

    // Allow Admin full visibility of all connections, inquiries, and orders for global management and cleanup
    match /connections/{connectionId} {
      allow read: if isSignedIn() && (request.auth.uid == resource.data.fromUserId || request.auth.uid == resource.data.toUserId || isAdmin());
      allow create: if isSignedIn() && (request.auth.uid == incoming().fromUserId || request.auth.uid == incoming().toUserId);
      allow update: if isSignedIn() && (request.auth.uid == existing().fromUserId || request.auth.uid == existing().toUserId);
      allow delete: if isSignedIn() && (request.auth.uid == resource.data.fromUserId || request.auth.uid == resource.data.toUserId || isAdmin());
    }

    match /inquiries/{inquiryId} {
      allow read: if isSignedIn() && (request.auth.uid == resource.data.buyerId || request.auth.uid == resource.data.sellerId || isAdmin());
      allow create: if isSignedIn() && (request.auth.uid == incoming().buyerId || request.auth.uid == incoming().sellerId);
      allow update: if isSignedIn() && (request.auth.uid == existing().buyerId || request.auth.uid == existing().sellerId);
      allow delete: if isSignedIn() && (request.auth.uid == resource.data.buyerId || request.auth.uid == resource.data.sellerId || isAdmin());
    }

    match /b2b_orders/{orderId} {
      allow read: if isSignedIn() && (request.auth.uid == resource.data.buyerId || request.auth.uid == resource.data.sellerId || isAdmin());
      allow create: if isSignedIn() && (request.auth.uid == incoming().buyerId || request.auth.uid == incoming().sellerId);
      allow update: if isSignedIn() && (request.auth.uid == existing().buyerId || request.auth.uid == existing().sellerId);
      allow delete: if isSignedIn() && (request.auth.uid == resource.data.buyerId || request.auth.uid == resource.data.sellerId || isAdmin());
    }

    match /notifications/{notificationId} {
      allow read: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && (existing().userId == request.auth.uid || isAdmin());
    }

    match /reviews/{reviewId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && isOwner(incoming().buyerId);
      allow update: if isSignedIn() && isOwner(existing().buyerId);
      allow delete: if isSignedIn() && (isOwner(resource.data.buyerId) || isAdmin());
    }

    match /popular_searches/{searchId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn();
    }

    match /userSessions/{sessionId} {
      allow read: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
      allow create: if isSignedIn() && isOwner(incoming().userId);
      allow delete: if isSignedIn() && (isOwner(resource.data.userId) || isAdmin());
    }
    
    match /test/{docId} {
      allow read: if true;
    }
  }
}
